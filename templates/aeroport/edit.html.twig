{% extends 'base.html.twig' %}

{% block title %}Modifier Aéroport | BeauxAirlines{% endblock %}

{% block body %}
<main class="container mx-auto px-4 py-8">
    <div class="max-w-2xl mx-auto">
        <h1 class="text-3xl font-bold mb-6">Modifier l'aéroport</h1>

        <form method="post" action="{{ path('app_aeroport_edit', {'id': aeroport.id}) }}" class="space-y-6 bg-white shadow-lg rounded-lg p-6">
            <div class="rounded-md">
                <label for="nom" class="block text-sm font-medium text-gray-700">Nom</label>
                <input type="text" id="nom" name="nom" value="{{ aeroport.nom }}" required
                       class="mt-1 block px-2 py-2 w-full border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div class="rounded-md">
                <label for="ville" class="block text-sm font-medium text-gray-700">Ville</label>
                <input type="text" id="ville" name="ville" value="{{ aeroport.ville }}" required
                       class="mt-1 block px-2 py-2 w-full border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div class="rounded-md">
                <label for="pays" class="block text-sm font-medium text-gray-700">Pays</label>
                <input type="text" id="pays" name="pays" value="{{ aeroport.pays }}" required
                       class="mt-1 block px-2 py-2 w-full border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div class="flex justify-end space-x-4">
                <a href="{{ path('app_aeroport') }}"
                   class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                    Annuler
                </a>
                <button type="submit"
                        class="bg-blue-600 hover:bg-blue-800 text-white font-bold py-2 px-4 rounded">
                    Enregistrer
                </button>
            </div>
        </form>
    </div>
</main>
{% endblock %}
